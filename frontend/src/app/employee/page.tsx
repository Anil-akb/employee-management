import Employee from "@/components/employee/Employee";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <Employee />
    </div>
  );
};

export default page;
