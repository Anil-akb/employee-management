import React from "react";
import AuthSignup from "./AuthSignup";

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <AuthSignup />
    </div>
  );
};

export default page;
